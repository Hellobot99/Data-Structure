#include <iostream>
#include <vector>
#include <map>
#include <string>
using namespace std;

int main() {

	map<string, int> amap;
	string word;
	getline(cin, word, '.');
	for (int i = 0; i < word.length(); i++) {

	}
}